// 题目描述
// 给定一个double类型的浮点数base和int类型的整数exponent。求base的exponent次方。
// 保证base和exponent不同时为0

function Power(base,exponent){
    return Math.pow(base,exponent);
}
//...刚好学了js的一些函数，这是基本的幂函数计算。所以js一些情况下还是蛮方便的